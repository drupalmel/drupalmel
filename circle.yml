---
dependencies:
  override:
    - bash -e <(curl -fsSL http://bit.ly/beetbuild)
    - composer install
test:
  override:
    # Lint code.
    - vendor/bin/dcr install && . ~/.profile && dcr
    # Run tests.
    - cd tests/behat && vendor/bin/behat
